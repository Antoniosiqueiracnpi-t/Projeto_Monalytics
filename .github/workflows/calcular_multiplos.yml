name: Calcular Múltiplos

on:
  workflow_dispatch:
    inputs:
      modo:
        description: 'Modo de seleção'
        required: true
        type: choice
        options:
          - 'quantidade'
          - 'ticker'
          - 'lista'
          - 'faixa'
        default: 'quantidade'

      quantidade:
        description: 'Quantidade (modo quantidade): 10, 50, 308=todas'
        required: false
        default: '10'

      ticker:
        description: 'Ticker único (modo ticker): ex: PETR4'
        required: false
        default: ''

      lista:
        description: 'Lista de tickers (modo lista): ex: KLBN11,SLCE3,AGRO3'
        required: false
        default: ''

      faixa:
        description: 'Faixa (modo faixa): ex: 1-50, 51-150, 151-308'
        required: false
        default: '1-50'

  push:
    paths:
      - 'balancos/**/dre_padronizado.csv'
      - 'balancos/**/bpa_padronizado.csv'
      - 'balancos/**/bpp_padronizado.csv'

  schedule:
    - cron: '0 6 * * 1'  # Segunda-feira às 06:00 UTC

jobs:
  calcular_multiplos:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    permissions:
      contents: write

    steps:
      - name: 📥 Baixar código
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Instalar bibliotecas
        run: |
          pip install --upgrade pip
          pip install pandas numpy

      - name: 🔍 Detectar modo de execução
        id: detect_mode
        run: |
          if [ "${{ github.event_name }}" == "push" ]; then
            echo "mode=auto" >> $GITHUB_OUTPUT

            CHANGED_FILES=$(git diff --name-only HEAD~1 HEAD 2>/dev/null || echo "")
            TICKERS=""

            for file in $CHANGED_FILES; do
              if [[ "$file" == balancos/*/*.csv ]]; then
                TICKER=$(echo "$file" | cut -d'/' -f2)
                if [ -n "$TICKER" ] && [[ ! "$TICKERS" == *"$TICKER"* ]]; then
                  if [ -n "$TICKERS" ]; then
                    TICKERS="$TICKERS,$TICKER"
                  else
                    TICKERS="$TICKER"
                  fi
                fi
              fi
            done

            echo "tickers=$TICKERS" >> $GITHUB_OUTPUT
            echo "Tickers detectados: $TICKERS"
          else
            echo "mode=manual" >> $GITHUB_OUTPUT
            echo "tickers=" >> $GITHUB_OUTPUT
          fi

      - name: 🚀 Executar cálculo de múltiplos (Manual/Schedule)
        if: steps.detect_mode.outputs.mode == 'manual' || github.event_name == 'schedule'
        run: |
          python src/calcular_multiplos.py \
            --modo "${{ github.event.inputs.modo || 'quantidade' }}" \
            --quantidade "${{ github.event.inputs.quantidade || '308' }}" \
            --ticker "${{ github.event.inputs.ticker || '' }}" \
            --lista "${{ github.event.inputs.lista || '' }}" \
            --faixa "${{ github.event.inputs.faixa || '1-308' }}"

      - name: 🚀 Executar cálculo de múltiplos (Auto - Push)
        if: steps.detect_mode.outputs.mode == 'auto' && steps.detect_mode.outputs.tickers != ''
        run: |
          python src/calcular_multiplos.py \
            --modo "lista" \
            --lista "${{ steps.detect_mode.outputs.tickers }}"

      - name: 📊 Validar saídas geradas (novo padrão por classe)
        run: |
          echo "=== Arquivos JS gerados (multiplos_*.js) ==="
          find balancos -type f -name "multiplos_*.js" 2>/dev/null | head -30 || true

          echo ""
          echo "=== Arquivos CSV gerados (multiplos_*.csv) ==="
          find balancos -type f -name "multiplos_*.csv" 2>/dev/null | head -30 || true

          echo ""
          echo "=== Contagem total (novo padrão) ==="
          JS_COUNT=$(find balancos -type f -name "multiplos_*.js" 2>/dev/null | wc -l || echo "0")
          CSV_COUNT=$(find balancos -type f -name "multiplos_*.csv" 2>/dev/null | wc -l || echo "0")
          echo "JS:  $JS_COUNT arquivos"
          echo "CSV: $CSV_COUNT arquivos"

          echo ""
          echo "=== (Legado, se existir) ==="
          LEG_JSON=$(find balancos -type f -name "multiplos.json" 2>/dev/null | wc -l || echo "0")
          LEG_CSV=$(find balancos -type f -name "multiplos.csv" 2>/dev/null | wc -l || echo "0")
          echo "JSON legado: $LEG_JSON arquivos"
          echo "CSV legado:  $LEG_CSV arquivos"

      - name: 💾 Commit e Push
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

          echo "🔄 Sincronizando com remote..."

          git add -A
          git stash --include-untracked || echo "Sem mudanças para stash"

          git pull origin main --no-rebase || {
            echo "⚠️ Pull falhou, tentando reset..."
            git fetch origin
            git reset --hard origin/main
          }

          git stash pop || echo "Stash vazio"

          # ====================================================================
          # PASSO 2: Adicionar APENAS arquivos de múltiplos (novo padrão)
          # ====================================================================
          git add balancos/*/multiplos_*.js balancos/*/multiplos_*.csv 2>/dev/null || true

          # (Opcional) legados
          git add balancos/*/multiplos.json balancos/*/multiplos.csv 2>/dev/null || true

          if git diff --cached --quiet; then
            echo "✅ Sem alterações nos múltiplos. Nada para commitar."
            exit 0
          fi

          CHANGED_COUNT=$(git diff --cached --name-only | wc -l)
          echo "📊 Arquivos alterados: $CHANGED_COUNT"

          echo "📝 Primeiros 15 arquivos:"
          git diff --cached --name-only | head -15

          TRIGGER="${{ github.event_name }}"
          DATE=$(date +'%Y-%m-%d %H:%M UTC')

          case "$TRIGGER" in
            "push")
              MSG="🤖 Múltiplos atualizados (auto) - $DATE"
              ;;
            "schedule")
              MSG="📅 Múltiplos atualizados (semanal) - $DATE"
              ;;
            *)
              MSG="🔢 Múltiplos calculados (manual) - $DATE"
              ;;
          esac

          git commit -m "$MSG"

          MAX_RETRIES=5
          RETRY_COUNT=0
          SLEEP_TIME=3

          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            echo "📤 Tentativa de push $((RETRY_COUNT + 1))/$MAX_RETRIES..."

            if git push origin HEAD:main; then
              echo "✅ Push realizado com sucesso!"
              exit 0
            else
              RETRY_COUNT=$((RETRY_COUNT + 1))

              if [ $RETRY_COUNT -lt $MAX_RETRIES ]; then
                echo "⚠️ Push falhou. Aguardando ${SLEEP_TIME}s antes de tentar novamente..."
                sleep $SLEEP_TIME

                echo "🔄 Sincronizando com remote (rebase)..."
                git fetch origin
                git rebase origin/main || {
                  echo "❌ Rebase falhou. Abortando e tentando merge..."
                  git rebase --abort
                  git pull origin main --no-rebase --no-edit
                }

                SLEEP_TIME=$((SLEEP_TIME + 2))
              fi
            fi
          done

          echo "❌ Push falhou após $MAX_RETRIES tentativas"
          echo "💡 Possível conflito com outro workflow. Verifique os logs."
          exit 1

      - name: 📋 Resumo da Execução
        if: always()
        run: |
          echo "=========================================="
          echo "🎯 RESUMO DA EXECUÇÃO"
          echo "=========================================="
          echo "Trigger: ${{ github.event_name }}"
          echo "Modo: ${{ steps.detect_mode.outputs.mode }}"

          if [ "${{ steps.detect_mode.outputs.mode }}" == "auto" ]; then
            echo "Tickers processados: ${{ steps.detect_mode.outputs.tickers }}"
          else
            echo "Modo manual: ${{ github.event.inputs.modo || 'quantidade' }}"
          fi

          echo ""
          echo "📊 Múltiplos finais no repositório (novo padrão):"
          JS_COUNT=$(find balancos -type f -name "multiplos_*.js" 2>/dev/null | wc -l || echo "0")
          CSV_COUNT=$(find balancos -type f -name "multiplos_*.csv" 2>/dev/null | wc -l || echo "0")
          echo "JS:  $JS_COUNT arquivos"
          echo "CSV: $CSV_COUNT arquivos"

          echo ""
          echo "📎 (Legado, se existir):"
          LEG_JSON=$(find balancos -type f -name "multiplos.json" 2>/dev/null | wc -l || echo "0")
          LEG_CSV=$(find balancos -type f -name "multiplos.csv" 2>/dev/null | wc -l || echo "0")
          echo "JSON legado: $LEG_JSON arquivos"
          echo "CSV legado:  $LEG_CSV arquivos"
          echo "=========================================="
