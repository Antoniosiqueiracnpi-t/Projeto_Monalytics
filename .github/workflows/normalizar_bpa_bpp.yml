name: Normalizar BPA e BPP

on:
  workflow_dispatch:
    inputs:
      modo:
        description: 'Modo de seleção'
        required: true
        default: 'quantidade'
        type: choice
        options:
          - quantidade
          - ticker
          - lista
          - faixa
      quantidade:
        description: 'Quantidade (modo quantidade): 10, 50, 308=todas'
        required: false
        default: '10'
      ticker:
        description: 'Ticker único (modo ticker): ex: PETR4'
        required: false
        default: ''
      lista:
        description: 'Lista de tickers (modo lista): ex: PETR4,VALE3,ITUB4'
        required: false
        default: ''
      faixa:
        description: 'Faixa (modo faixa): ex: 1-50, 51-150, 151-308'
        required: false
        default: '1-50'
  schedule:
    - cron: "25 3 * * *"  # 3:25 AM (APENAS BPA/BPP, 10min após DRE)
  push:
    paths:
      - "balancos/**"
      - "src/**"
      - "padronizar_bpa_bpp.py"
      - ".github/workflows/normalizar_bpa_bpp.yml"

permissions:
  contents: write

concurrency:
  group: balancos-bpa-bpp
  cancel-in-progress: false

jobs:
  normalizar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy requests

      - name: Run normalization (BPA/BPP ONLY)
        run: |
          export PYTHONPATH="$GITHUB_WORKSPACE"
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            python src/normalize_bpp_bpa.py \
              --modo "${{ github.event.inputs.modo }}" \
              --quantidade "${{ github.event.inputs.quantidade }}" \
              --ticker "${{ github.event.inputs.ticker }}" \
              --lista "${{ github.event.inputs.lista }}" \
              --faixa "${{ github.event.inputs.faixa }}"
          else
            python src/normalize_bpp_bpa.py
          fi

      - name: Commit and push if changed
        run: |
          git config user.name "actions-user"
          git config user.email "actions@users.noreply.github.com"

          git add -A balancos

          if git diff --cached --quiet; then
            echo "Sem mudanças para commit."
            exit 0
          fi

          git commit -m "Normalização BPA/BPP padronizada (todas as pastas)"

          git pull --rebase origin main
          git push origin main || (git pull --rebase origin main && git push origin main)
