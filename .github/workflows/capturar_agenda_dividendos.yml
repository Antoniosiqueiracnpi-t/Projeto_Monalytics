name: Capturar Agenda Dividendos Completa

on:
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:
    inputs:
      modo:
        description: 'Modo de captura'
        required: true
        default: 'quantidade'
        type: choice
        options:
          - quantidade
          - ticker
          - lista
          - completo
      quantidade:
        description: 'Quantidade de empresas (modo quantidade)'
        required: false
        default: '10'
      ticker:
        description: 'Ticker específico (modo ticker)'
        required: false
        default: ''
      lista:
        description: 'Lista de tickers separados por vírgula (modo lista)'
        required: false
        default: ''

jobs:
  capturar-agenda-dividendos:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v3
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Instalar dependências
        run: |
          pip install finbr pandas requests --break-system-packages
      
      - name: Capturar dividendos históricos
        run: |
          echo "📊 Capturando dividendos históricos (finbr)..."
          
          if [ "${{ github.event_name }}" == "schedule" ]; then
            echo "🤖 Execução automática: modo completo"
            python src/capturar_dividendos_passados.py --modo completo
          else
            echo "👤 Execução manual: modo ${{ github.event.inputs.modo }}"
            
            if [ "${{ github.event.inputs.modo }}" == "ticker" ]; then
              python src/capturar_dividendos_passados.py \
                --modo ticker \
                --ticker "${{ github.event.inputs.ticker }}"
            
            elif [ "${{ github.event.inputs.modo }}" == "lista" ]; then
              python src/capturar_dividendos_passados.py \
                --modo lista \
                --lista "${{ github.event.inputs.lista }}"
            
            elif [ "${{ github.event.inputs.modo }}" == "quantidade" ]; then
              python src/capturar_dividendos_passados.py \
                --modo quantidade \
                --quantidade "${{ github.event.inputs.quantidade }}"
            
            elif [ "${{ github.event.inputs.modo }}" == "completo" ]; then
              python src/capturar_dividendos_passados.py --modo completo
            fi
          fi
      
      - name: Capturar dividendos futuros
        run: |
          echo "🔮 Capturando dividendos futuros (notícias + B3 API)..."
          
          if [ "${{ github.event_name }}" == "schedule" ]; then
            echo "🤖 Execução automática: modo completo"
            python src/capturar_dividendos_futuros.py --modo completo
          else
            echo "👤 Execução manual: modo ${{ github.event.inputs.modo }}"
            
            if [ "${{ github.event.inputs.modo }}" == "ticker" ]; then
              python src/capturar_dividendos_futuros.py \
                --modo ticker \
                --ticker "${{ github.event.inputs.ticker }}"
            
            elif [ "${{ github.event.inputs.modo }}" == "lista" ]; then
              python src/capturar_dividendos_futuros.py \
                --modo lista \
                --lista "${{ github.event.inputs.lista }}"
            
            elif [ "${{ github.event.inputs.modo }}" == "quantidade" ]; then
              python src/capturar_dividendos_futuros.py \
                --modo quantidade \
                --quantidade "${{ github.event.inputs.quantidade }}"
            
            elif [ "${{ github.event.inputs.modo }}" == "completo" ]; then
              python src/capturar_dividendos_futuros.py --modo completo
            fi
          fi
      
      - name: Consolidar agenda de dividendos
        run: |
          echo "📦 Consolidando dividendos em agenda única..."
          python src/consolidar_dividendos.py
      
      - name: Verificar arquivos gerados
        run: |
          echo "🔍 Verificando arquivos gerados..."
          
          if [ -f "balancos/agenda_dividendos.json" ]; then
            echo "✅ agenda_dividendos.json criado"
            TAMANHO=$(du -h balancos/agenda_dividendos.json | cut -f1)
            echo "   Tamanho: $TAMANHO"
          else
            echo "⚠️  agenda_dividendos.json não encontrado"
          fi
          
          if [ -f "balancos/agenda_dividendos_mes.json" ]; then
            echo "✅ agenda_dividendos_mes.json criado"
          fi
          
          if [ -f "balancos/estatisticas_dividendos.json" ]; then
            echo "✅ estatisticas_dividendos.json criado"
          fi
          
          HISTORICO_COUNT=$(find balancos -name "dividendos_historico.json" | wc -l)
          FUTUROS_COUNT=$(find balancos -name "dividendos_futuros.json" | wc -l)
          
          echo ""
          echo "📊 Resumo:"
          echo "   Dividendos históricos: $HISTORICO_COUNT empresas"
          echo "   Dividendos futuros: $FUTUROS_COUNT empresas"

      - name: 📝 Atualizar timestamp
        run: |
          mkdir -p site/data
          
          TIMESTAMP=$(date +%s)000
          DATA=$(TZ='America/Sao_Paulo' date +'%Y-%m-%d')
          HORA=$(TZ='America/Sao_Paulo' date +'%H:%M BRT')
          
          cat > site/data/ultima_atualizacao.json << EOF
          {
            "timestamp": ${TIMESTAMP},
            "data": "${DATA}",
            "hora": "${HORA}",
            "fonte": "agenda_dividendos",
            "versao": "1.0.0"
          }
          EOF
          
          echo "✅ Timestamp atualizado: ${TIMESTAMP} (${DATA} ${HORA})"
      
      - name: Commit e Push resultados
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          git add balancos/*/dividendos_historico.json 2>/dev/null || true
          git add balancos/*/dividendos_futuros.json 2>/dev/null || true
          git add balancos/agenda_dividendos.json 2>/dev/null || true
          git add balancos/agenda_dividendos_mes.json 2>/dev/null || true
          git add balancos/estatisticas_dividendos.json 2>/dev/null || true
          git add site/data/ultima_atualizacao.json
          
          if git diff --staged --quiet; then
            echo "⚠️  Nenhuma mudança para commit"
            exit 0
          fi
          
          if [ "${{ github.event_name }}" == "schedule" ]; then
            COMMIT_MSG="💰 Atualizar agenda completa de dividendos (automático - $(date +'%Y-%m-%d %H:%M')) [skip netlify]"
          else
            COMMIT_MSG="💰 Atualizar agenda completa de dividendos - ${{ github.event.inputs.modo }} [skip netlify]"
          fi
          
          git commit -m "$COMMIT_MSG"
          git push
          
          echo "✅ Arquivos commitados e enviados com sucesso!"
